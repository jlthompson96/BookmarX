pipeline:
  identifier: Build_BookmarX_Backend_1698430709822
  name: Build BookmarX_Backend
  orgIdentifier: default
  projectIdentifier: BookmarX
  properties:
    ci:
      codebase:
        build: <+input>
        connectorRef: account.Github
        repoName: BookmarX_Backend
  stages:
    - stage:
        identifier: build
        name: build
        spec:
          cloneCodebase: true
          execution:
            steps:
              - step:
                  identifier: echo
                  name: echo
                  spec:
                    command: echo hello world
                  timeout: ""
                  type: Run
          platform:
            arch: Amd64
            os: Linux
          runtime:
            spec: {}
            type: Cloud
        type: CI
